{
  "name": "prompts-repository",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd gemini-clone && npm install && npm run dev",
    "build": "cd gemini-clone && npm install && npm run build",
    "start": "cd gemini-clone && npm install && npm start"
  }
}
